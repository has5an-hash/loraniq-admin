# داده‌های آماده ثبت محصول لورانیک در راست‌چین

> این سند **متن نهایی تبلیغاتی نیست**. فقط داده‌های فنی اثبات‌شده را برای ثبت محصول و نویسنده/طراح انسانی جمع می‌کند.

## هویت محصول

- نام فارسی: لورانیک ادمین
- نام انگلیسی: Loraniq Admin
- نسخه: `1.0.0`
- نوع محصول: قالب رابط کاربری پنل مدیریت React / Next.js
- دسته پیشنهادی بر پایه نمونه‌های جاری مارکت: قالب مدیریت HTML
- زبان و جهت: فارسی، RTL

## قابلیت‌های اثبات‌شده

- ۱۰ فضای کاری/دموی تعاملی
- داشبورد تجارت
- تحلیل داده
- مدیریت پروژه
- امور مالی
- CRM
- آکادمی
- تقویم
- پیام‌ها
- کتابخانه کامپوننت‌ها
- تنظیمات حساب
- حالت روشن و تیره
- Responsive موبایل و دسکتاپ
- رابط فارسی و RTL

## فناوری‌های نسخه 1.0.0

- React `19.3.0`
- Next.js `16.3.4`
- TypeScript `5.9.3`
- Vinext `1.0.0-beta.9`
- Vite `8.2.2`
- Wrangler `4.130.0`
- Recharts
- Node.js موردنیاز: `>=22.13.0`

## فایل محصول

نام فایل تجاری تولیدشده توسط CI:

`Loraniq-Admin-RTLTheme-1.0.0.zip`

فایل معتبر باید از Artifact آخرین Release QA سبز گرفته شود؛ ZIPهای قدیمی موجود در تاریخچه مرجع ارسال نیستند.

## شواهد QA

QA خودکار شامل نصب تمیز، audit وابستگی production، lint، TypeScript، Release Gate، Build، اجرای Preview، تست HTTP تمام مسیرها، Screenshot دسکتاپ/موبایل، Overflow Gate، ساخت ZIP و Restore-test خود ZIP است.

Interaction QA نیز Dark/Light، جستجو، منوی موبایل، HTTP resource errors، page errors و overflow را روی هر ۱۰ دمو کنترل می‌کند.

## دمو عمومی

Static Export برای GitHub Pages با موفقیت ساخته شده و تمام ۱۰ دمو prerender می‌شوند.

مرحله باقی‌مانده برای انتشار عمومی:
1. در GitHub مخزن `loraniq-admin` وارد `Settings > Pages` شوید.
2. در `Build and deployment`، Source را روی `GitHub Actions` قرار دهید.
3. Workflow با نام `Loraniq Public Demo` دوباره اجرا شود.
4. URL عمومی پس از Deploy باید جداگانه باز و QA شود.

تا قبل از Deploy موفق، هیچ URL حدسی به راست‌چین داده نشود.

## موارد ممنوع برای ادعا

- بیش از ۱۰ دمو مگر نسخه جدید واقعاً اضافه و QA شود
- «۱۲۰+ صفحه» یا اعداد مشابه بدون شمارش واقعی
- امتیاز، فروش، تعداد کاربر یا رضایت ساختگی
- Backend آماده، API آماده یا احراز هویت سازمانی
- سازگاری‌ای که تست نشده است
- لایسنس فنی React/Next که روش رسمی آن تأیید نشده باشد

## اقلامی که باید توسط انسان/حساب فروشنده تکمیل شوند

- متن نهایی صفحه محصول مطابق استاندارد جاری راست‌چین
- کاور نهایی انسانی و مجاز
- اینفوگرافیک انسانی با عرض ۹۶۰px و حجم کمتر از ۱MB
- اطلاعات فروشنده و پشتیبانی
- URL دموی عمومی بعد از Deploy واقعی
- تأیید روش لایسنس فنی فقط در صورت الزام پنل راست‌چین
